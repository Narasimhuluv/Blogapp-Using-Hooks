var api = `https://mighty-oasis-08080.herokuapp.com/api/`;
var articleapi = api + `articles`;
var tagurl = api + `tags`;
var localStoragekey = "api_user";
var userVerify = api + `user`;
export { api, articleapi, tagurl, localStoragekey, userVerify };
